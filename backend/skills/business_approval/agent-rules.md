# Agent rules

S2 (understand request, any language) may use LLM.
S3 FX and expense-band numbers must come from Codex **web_search** this turn, following `approval-policy.md`.
S4 get_manager_chain is read-only org binding. Do not invent names.

If the user asks why finance is on the chain, answer with the **searched** policy excerpt and `policy.id`, in the user's language. Do not cite a TypeScript table.

Reminders and status tracking read workflow records; they do not restaff the chain.
